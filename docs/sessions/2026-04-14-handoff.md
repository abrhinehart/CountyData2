# Handoff Prompt — CountyData2 (2026-04-14)

Paste this into a fresh Claude Code session to continue where the prior session left off.

---

You are picking up a CountyData2 working session from 2026-04-14. The full journal for that
session lives at `docs/sessions/2026-04-14-project-reload.md` — read it first for context,
especially the "Items NOT done" section.

**Branch:** `claude/silly-cohen` (check out if you are not already on it).
**Working directory:** `C:\Users\arhinehart\Code\CountyData2\.claude\worktrees\silly-cohen`.
**Expected baseline:** `pytest` should report **457 passing** on a clean checkout of this branch
after the latest commit has landed. If it does not, stop and investigate before making changes.
Install dev dependencies with `pip install -r requirements-dev.txt` inside the repo's `.venv`.

## What was finished last session

17 items spanning a test-suite baseline, Commission Roster (CR) and Permit Tracker (PT)
jurisdiction activations, a registry drift-prevention test, a lazy DB-pool refactor, and
requirements cleanup. CR validated jurisdiction count went 3 -> 12; PT validated count went
3 -> 7 plus the Tyler cluster of 4 more. See the journal for the full list.

## Four deferred items

1. **Haines City production throttling.** The iWorQ adapter for Haines City fires a detail-page
   GET per row because the grid has no permit-type column. Production fan-out risk. Consider
   scheduler-level rate limiting or adapter-level batching.
   - Adapter: `modules/permits/adapters/haines_city_iworq.py` (look here first).
   - Validation fixture/tests: `tests/test_haines_city_adapter.py`.

2. **YAML `category_id` audit across ~97 FL commission configs.** Two confirmed drifts last
   session: Sumter BCC (fixed: `5 -> 3`) and Lake Wales CC (partial: `category_id=3` returns
   the wrong board). Walk the full YAML set and verify each live.
   - Configs: `modules/commission/configs/*.yaml` (FL only first pass).
   - Harness: `scripts/cr_live_validate.py`.

3. **Lake Wales CC `category_id` refinement.** Currently pulls Planning and Zoning Board, not
   City Commission. Needs portal recon to identify the correct ID, then a YAML fix plus a
   re-validation run with the harness.
   - Config: `modules/commission/configs/lake-wales-cc.yaml` (or similarly named).
   - Recon notes: `docs/commission/live-validation/`.

4. **HEAD/GET asymmetry detection pattern.** Bay County BCC's NovusAgenda portal returns 200
   on HEAD while the GET body is an error template. No reusable helper exists yet. Consider a
   small utility (e.g. `shared/portal_health.py`) that does a real GET and pattern-matches the
   body, then wire it into whatever uptime check CR currently runs.
   - Existing recon: `docs/commission/live-validation/` (Bay entries).

## Suggested starting moves

- If you want a fresh audit before touching anything, invoke the
  `anthropic-skills:project-reload` skill. It will surface anything that drifted since the
  journal was written.
- Otherwise pick one of the four items above. Item 3 is the smallest and highest-signal start.
  Item 2 is the largest; plan to batch it via the `triad-agent-v2` skill.
- For any jurisdiction validation work, reuse `scripts/cr_live_validate.py` and follow the Tyler
  pattern (live scrape -> anonymized fixture -> adapter test -> drift check all together).

## Guardrails to remember

- Don't propose MS county work without an explicit request.
- "BOCC" is ambiguous in this codebase; verify the actual Accela agency code before keying by
  abbreviation. (Charlotte FL uses `CHARLOTTEFL`.)
- When stubbing out adapters, flip the registry metadata at the same time. The
  `tests/test_registry_code_consistency.py` drift check catches most structural lies but not
  semantic ones.
