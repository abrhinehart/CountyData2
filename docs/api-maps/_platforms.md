# API Mapping — Platforms Registry

Living list of county-government web platforms observed across all mapped jurisdictions. Used as the source of truth for platform fingerprinting (see county-api-mapping-instructions.md §5). Fingerprint once per hostname — if a hostname appears here, skip re-discovery and jump to probing the known endpoint patterns.

When a new platform is discovered during a mapping run, add a row. Do not delete rows: if a platform is no longer used anywhere, leave it — the entry documents the signatures that would still identify it if it reappears.

## Columns

- **Platform** — product name as the vendor brands it.
- **Vendor** — parent company (often different from the product name).
- **Hostnames** — observed URL patterns (subdomains, root domains). Wildcards welcome.
- **Signatures** — HTML/header/cookie markers that fingerprint the platform in seconds.
- **Known endpoint patterns** — paths worth probing even when the front-end UI doesn't link to them.
- **Adapter in repo** — path (relative to repo root) of the scraper / client module, if we have one.

## Registry

| Platform | Vendor | Hostnames | Signatures | Known endpoint patterns | Adapter in repo |
|---|---|---|---|---|---|
| **Legistar** | Granicus | `*.legistar.com` | "Meeting Portal" branding, `WebSessionID` cookie | `/View.ashx`, `/Calendar.aspx`, `/DepartmentDetail.aspx`, `/MeetingDetail.aspx`, `/LegislationDetail.aspx` | `cr/adapters/legistar.py` |
| **CivicClerk** | Diligent / CivicPlus | `*.civicclerk.com` | `civicclerk` in script srcs, `/Web/GenericHooks` | `/Web/GenericHooks`, `/api/v1/Meetings`, `/api/v1/Events` | `cr/adapters/civicclerk.py` |
| **CivicWeb / iCompass** | iCompass (Diligent) | `*.civicweb.net` | `CivicWeb` footer, ASP.NET viewstate | `/Documents/Filings`, `/Documents/GenericGrid`, RSS at `/Documents.aspx?rss` | `cr/adapters/civicweb.py` |
| **eScribe** | eSCRIBE Software | `pub-*.escribemeetings.com` | `escribe` in URL, JSON POSTs to `/MeetingsCalendarView.aspx/GetCalendarMeetings` | `/MeetingsCalendarView.aspx/GetCalendarMeetings`, `/Meetings.aspx?Id=…` | `cr/adapters/escribe.py` |
| **NovusAgenda** | Novus Systems | `*.novusagenda.com` | `DisplayAgendaPDF.ashx` anchor pattern, `agendapublic/` path | `/agendapublic/`, `/agendapublic/DisplayAgendaPDF.ashx?…`, `/agendapublic/meetingsearch.aspx` | `cr/adapters/novusagenda.py` |
| **iQM2 / MinutesOnDemand** | Granicus | `*.iqm2.com` | `iqm2` in URL, "MinutesOnDemand" branding | `/Citizens/Board.aspx`, `/Citizens/Meeting.aspx`, RSS feeds | `cr/adapters/iqm2.py` |
| **CivicPlus Agenda Center** | CivicPlus | `*.civicplus.com`, customer domains with `/AgendaCenter/` | "Powered by CivicPlus" footer, `/AgendaCenter/ViewFile/Agenda/…` | `/AgendaCenter/`, `/AgendaCenter/Search/`, `/ArchiveCenter/` | `cr/adapters/civicplus.py` |
| **Granicus ViewPublisher** | Granicus | `*.granicus.com`, customer domains | `ViewPublisher.php`, "powered by Granicus" | `/ViewPublisher.php`, `/MediaPlayer.php`, `/api/` | `cr/adapters/granicus.py` |
| **BoardDocs** | Diligent | `go.boarddocs.com`, `*.boarddocs.com` | `boarddocs` in URL, iframe-embedded portal | `/public/` JSON endpoints (undocumented, discovered via XHR) | _(none yet)_ |
| **PeakAgenda** | Peak Democracy / OpenGov | `*.peakagenda.com` | `peakagenda` in URL | `/api/meetings`, `/api/agenda-items` | _(none yet)_ |
| **OnBase** | Hyland | `onbase` in URL | Hyland branding, `/AppNet/`, `/OnBaseHttp/` | `/AppNet/`, `/OnBaseHttp/` | _(none yet)_ |
| **ArcGIS REST** | Esri | `*.arcgis.com`, `/arcgis/rest/services/` | any `?f=json` response, FeatureServer / MapServer | `/arcgis/rest/services/?f=json`, `/FeatureServer/`, `/MapServer/` | `bi/clients/arcgis.py` |
| **OpenGov** | OpenGov | `*.opengov.com` | "Powered by OpenGov", `/budget/`, `/checkbook/` | `/api/v2/`, budget and checkbook endpoints | _(none yet)_ |
| **Tyler EnerGov (SelfService)** | Tyler Technologies | customer domains with `/EnerGovProd/SelfService/` or `/energovpub/selfservice/` | "Powered by Tyler Technologies", EnerGov in URL | `/EnerGovProd/SelfService/#/home`, `/energovpub/selfservice/api/energov/`, case-search JSON POSTs | `pt/adapters/tyler_energov.py` |
| **Tyler Munis SelfServe** | Tyler Technologies | `*.tylertech.com`, `selfservice.*` | MUNIS branding, session cookies with `MUNIS` prefix | `/MSS/`, `/api/public/` | _(none yet)_ |
| **Accela Citizen Access** | Accela | `aca-prod.accela.com/<AGENCY>/*`, `aca.*`, `*.accela.com`. **Known Polk-area tenants (per-jurisdiction agency codes):** `POLKCO` (Polk County — also used by Eagle Lake, Fort Meade, Polk City via delegation), `COWH` (City Of Winter Haven — see ACCELA-16), `COLA` (City Of Lake Alfred), `MULBERRY` (City of Mulberry — self-hosted, NOT delegated to POLKCO). Tenant code typically matches `x-accela-traceId: aca-<tenant-lower>-...` response header. | "Accela Citizen Access" banner, `/apo/` path, `x-accela-traceId` response header names the tenant, ASPX + ApplicationGatewayAffinity + `.ASPXANONYMOUS` + `ACA_SS_STORE` + `ACA_USER_PREFERRED_CULTURE` cookies | `/<AGENCY>/Default.aspx`, `/<AGENCY>/Cap/CapHome.aspx?module=<Building|Enforcement|Planning|Licenses|LandDev>`, `/<AGENCY>/Cap/CapDetail.aspx?Module=<M>&TabName=<T>&capID=X-Y-Z`, `/portlets/cap/global/`, `/apo/`, `/v4/agency/<AGENCY>` (REST blocked anon) | `pt/adapters/accela_html.py` (REST v4 blocked anon, see `accela-rest-probe-findings.md`) |
| **AcclaimWeb** | Acclaim Imaging | `acclaimweb.com`, `acclaim-*.` | "Acclaim Web" header, `/AcclaimWeb/search/` | `/AcclaimWeb/search/SearchTypeName`, `/AcclaimWeb/Image/Thumbnail` | `pt/adapters/acclaimweb.py` |
| **OnCore Solutions (Landmark/Clerk)** | OnCore Solutions | `oncoreweb.*`, `*.oncoresolutions.com`, `*.landmarkweb.net`, `landmark.*` | "Landmark Web" branding, `/OnCoreWeb/` path | `/OnCoreWeb/SearchAvailable`, `/OnCoreWeb/Home` | `cd2/adapters/oncore_landmark.py` |
| **MGO-Connect** | MGO | `*.mgoconnect.com` | "MGO Connect" branding | varies per tenant, usually `/api/` | _(none yet)_ |
| **PermitTrax** | ePermit Solutions | `permittrax.*` | "PermitTrax" branding | `/PermitTrax/`, `/api/permit` | _(none yet)_ |
| **BITCO (BS&A style)** | Bitco Software | `bitco.net`, customer domains | "BITCO" footer | `/apps/`, per-customer variations | _(none yet)_ |
| **CityView** | Harris Computer | `*.cityview.com`, customer domains with `/CityViewPortal/` | "CityView Portal" branding | `/CityViewPortal/`, `/CityViewPortal/services/` | `pt/adapters/cityview.py` |
| **Municode Library (Codes)** | CivicPlus | `library.municode.com`, `api.municode.com` | `library.municode.com` URL, `api.municode.com` JSON | `api.municode.com/codes/{client_id}/nodes`, `IsUpdated`, `IsAmended`, `HasAmendedDescendant` flags | `cd2/adapters/municode.py` |
| **American Legal Publishing** | American Legal | `codelibrary.amlegal.com`, `*.amlegal.com` | "American Legal" footer | `/nxt/gateway.dll/`, `/api/` | _(none yet)_ |
| **Catalis GovOffice** | Catalis | customer domains with `/repository/designs/templates/GO_*` | classic ASP (`.asp`), `ASPSESSIONID*` cookies, `/vertical/sites/%7B<guid>%7D/uploads/` | `/index.asp?SEC=<GUID>`, `/index.asp?SEC=<GUID>&DE=<GUID>` | _(ad-hoc, see `davenport-fl.md`)_ |
| **WordPress REST** | WordPress / Automattic | any WP-hosted customer domain | `Link: <…>; rel="https://api.w.org/"` response header, `/wp-json/` returns site descriptor JSON, `/wp-sitemap.xml` index, `xmlrpc.php`, `rel="pingback"` link | `/wp-json/`, `/wp-json/wp/v2/{pages,posts,media,categories,tags,users,search,types,taxonomies}`, `/wp-json/wp/v2/<custom_post_type>`, `/feed/`, `/wp-sitemap.xml`. Collection max `per_page=100`. Headers `X-WP-Total`, `X-WP-TotalPages` on every paged response. | _(none yet — see `dundee-fl.md`)_ |
| **The Events Calendar (Tribe)** | StellarWP / Liquid Web | WordPress customer domains running the plugin | `<meta name="tec-api-version" content="v1">`, `<link rel="alternate" href="…/wp-json/tribe/events/v1/">`, `/events/?ical=1` iCal feed with `PRODID:-//…//ECPv<N>`, REST namespaces `tribe/events/v1`, `tribe/views/v2`, `tribe/event-aggregator/v1`, `tec/v2/onboarding` | `/wp-json/tribe/events/v1/{events,venues,organizers,categories,tags}`, `/events/v1/events/{id}`, `/events/v1/events/by-slug/{slug}`, `/events/?ical=1`, `/wp-json/wp/v2/{tribe_events,tribe_venue,tribe_organizer,tribe_events_cat,tec_calendar_embed}` | _(none yet — see `dundee-fl.md`)_ |
| **Municode Meetings** | CivicPlus (Municode) | `*.municodemeetings.com`, `meetings.municode.com`, `mccmeetings.blob.core.usgovcloudapi.net/<tenant>-pubu/` | Agenda/packet PDFs at `mccmeetings.blob.core.usgovcloudapi.net/<tenant>-pubu/MEET-{Agenda,Packet,Minutes}-<guid>.pdf`, ADA-HTML at `meetings.municode.com/adaHtmlDocument/index?cc=<TENANT>&me=<guid>`, Drupal-based tenant site with body-filter paths like `/bc-tc/page/<event-slug>` | `/calendar`, `/bc-<body>/page/<event-slug>`, ADA HTML + blob PDFs — full REST contract not yet probed | _(none yet — see `dundee-fl.md`)_ |
| **Authority Pay** | Authority Payments (muni-utility payment portal) | `<tenant>.authoritypay.com` (per-tenant subdomain; `lakehamilton.authoritypay.com` observed as outbound link on the Lake Hamilton Town CMS) | Tenant-subdomain pattern on apex `authoritypay.com`; referenced as the "Pay Utility Bill" outbound link from the CivicPlus CMS; no data surface deep-probed — HTML/cookie signatures not yet characterized (⚠️ GAP) | Not yet probed (payment-only outbound portal; no known public read API) | _(none — payment-only outbound; no adapter planned)_ |
| **NewVision Systems (Clerk Official Records)** | NewVision Systems Corporation | customer subdomains on the clerk's apex (observed: `apps.polkcountyclerk.net/browserviewor/`) | "© NewVision Systems Corporation" footer; ASP.NET WebForms shell; URL segment `/browserviewor/` (Browser View — Official Records); multi-mode search landing exposes party-name / document-type / date-range / property-ID / board-commission search forms without login. No captcha observed on landing; login section coexists with public search controls. | Search landing on `/browserviewor/` root; deeper search paths (Search.aspx variants) 404 on Polk — suggests the landing's own controls post back via `__doPostBack` rather than separate URLs. Full XHR enumeration deferred to browser pass. ⚠️ GAP: API paths not yet characterized. | _(none yet — see `polk-county-fl.md`)_ |
| **Tyler Odyssey PRO (Civil / Court Records)** | Tyler Technologies | per-tenant subdomains (observed: `pro.polkcountyclerk.net/PRO`) | "PRO Release Version 1.2.4.0" in footer; Tyler branding; `Public Access` landing that gates search behind an acknowledgement click but no captcha; distinct from Tyler MSS / EnerGov / Eagle. Criminal records often live at a sibling subdomain (e.g., Polk splits criminal to `www.polkcountyclerk.net/299`). | `/PRO` root with `PublicAccess` flow; detailed XHR / API surface not yet characterized. ⚠️ GAP: full probe deferred. | _(none yet — see `polk-county-fl.md`)_ |
| **GovBuilt** | MCC Innovations | `<tenant>.govbuilt.com` (observed real tenant: `auburndalefl.govbuilt.com`); vendor URL `mccinnovations.com/solution/government-websites`. **⚠ Wildcard DNS:** every `<slug>.govbuilt.com` returns HTTP 200 — a 200 alone does NOT confirm a real tenant. **Detection discipline:** (a) homepage `<title>` — real tenant reads `<title>City of <Name></title>` (or similar municipal name), placeholder reads `<title>GOVBUILT PLATFORM - Tomorrow's Government Built Today</title>`. (b) body size — real tenants ~28-30 KB city-branded; placeholders ~31.6 KB identical generic content. (c) API probe — `GET /PublicReport/GetAllContentToolModels` returns **200 + JSON** on real tenants, **404 + ~8.1 KB error page** on placeholders. Verified 2026-04-18 across 19 Polk-city slug variants; 6 already-mapped Polk cities (Davenport, Winter Haven, Haines City, Dundee, Lake Hamilton, Lake Alfred) are all placeholder, not tenants. | Orchard Core ASP.NET stack; paths `/GovBuilt.FroalaEditor/`, `/GovBuilt.Resource/`, `/OrchardCore.Case/`, `/OrchardCore.License/`; `/AdvancedForms`, `/activitysearchtool`, `/licensed-entities` linked from the public landing; `ARRAffinity*` cookies (Azure App Service); Cloudflare fronted. Public JSON APIs unauthenticated. | `/PublicReport/GetAllContentToolModels?contentType=case|license&Start=&Length=&SortBy=&SortType=&...` (DataTables JSON; `recordsTotal`/`recordsFiltered`/`data[]`; confirmed `Length=1000` uncapped), `/PublicReport/PublicReport/GetAllowedCaseType?contentType=CaseType`, `/PublicReport/PublicReport/GetAllowedLicenseType?contentType=LicenseType`, `/PublicReport/PublicReport/GetCaseSubTypesByTypeId?caseTypeId=…`, `/PublicReport/PublicReport/GetLicenseSubTypesByTypeId?licenseTypeId=…`, `/PublicReport/PublicReport/GetCaseOrLicenseInspectionsByCaseIdOrLicenseId?caseIdOrLicenseId=…&isCase=…` (needs POST/anti-forgery), `/PublicReport/GetAllCaseDetailsData/{contentItemId}` (HTML detail), `/SearchRecommendations?Terms=…`. Applicant fields include `name`, `phoneNumber`, `applicantFirstName`, `applicantLastName`, `businessName`, `applicationLocation` (JSON-embedded address+lat/lng) — PII-dense. | _(none yet — see `auburndale-fl.md`)_ |
| **Laserfiche WebLink 10** | Laserfiche | `weblink.<tenant>.com`, `weblink.<tenant>.gov`; legacy `/weblink8/` path auto-redirects to `/WebLink/` (observed: `weblink.auburndalefl.com`, database `CityOfAuburndale`) | "Laserfiche Public Portal" alt text + `/WebLink/` virtual dir; `/WebLink/styles.css?v=10.2` version tag; Angular SPA shell (`bundles/app.bundle.js`, `bundles/dependencies.bundle.js`); cookies required (`MachineTag`, `AcceptsCookies`, `lastSessionAccess`, `WebLinkSession`); Microsoft-IIS/10.0 server. Angular services include `FolderListingService`, `DocumentService`, `SearchService`, `CustomSearchService`, `ContextService`. | Public-facing SPA entry points: `/WebLink/Browse.aspx?dbid=0&startid=<folderId>`, `/WebLink/DocView.aspx?id=<docId>`, `/WebLink/Search.aspx`, `/WebLink/CustomSearch.aspx?SearchName=<name>`, `/WebLink/PdfViewer.aspx?file=…`, `/WebLink/EmailDocument.aspx?id=…`, `/WebLink/MyWebLink.aspx`. Underlying JSON API is obfuscated in the Angular bundle and not enumerated yet — deep probe requires browser session. ⚠️ GAP: raw API routes not yet characterized. | _(none yet — see `auburndale-fl.md`)_ |
| **American Data Group (ADG) UBS** | American Data Group | `utility.<tenant>.com/ubs1/` (observed: `utility.auburndalefl.com/ubs1/`) | HTML meta `<meta name="author" content="Mark Newnham (kmn@adginc.net)">` and `<meta name="copyright" content="(c)2016 American Data Group">`; asset paths `/adg-custom-site/ubs/<orgId>/`, `/adg/supporting/`; login form fields `a_no` (account number), `a_pass`; Microsoft-IIS/10.0 + PHP/8.3. Login-gated utility billing portal. | Login-gated; no public data surface observed. ⚠️ GAP: not deep-probed (deferred per Lake Alfred rule — login-only). | _(none — payment/account-only outbound; no adapter planned)_ |
| **SmartGov Public Portal** | Granicus (formerly Dude Solutions / Brightly) | `<tenant>.smartgovcommunity.com`, `<tenant>.validation.smartgovcommunity.com` (observed: `ci-auburndale-fl.validation.smartgovcommunity.com` — `.validation.` subdomain is a staging/tenant-validation environment) | Landing page banner "Powered by Granicus" + "SmartGov Public Portal by Granicus"; ASP.NET (`X-Powered-By: ASP.NET`); navigation paths `/Public/Home`, `/Public/Account`, `/Public/BannerImage`, `/Public/DocumentsView`, `/Public/PrivacyPolicy`, `/Public/RefundPolicy`, `/Public/ReportsView`. | Public landing exposes Permits / Applications / Licenses / Reports tiles; deep case search requires authenticated session. ⚠️ GAP: unauthenticated JSON surface not yet characterized; Auburndale's tenant is on the `.validation.` staging subdomain and the construction-services CMS links to GovBuilt as the live portal. | _(none yet — see `auburndale-fl.md`)_ |
| **Column (Public Notices)** | Column, PBC | `<tenant>.column.us` (observed: `cityoflakewales.column.us`); apex `www.column.us` is the marketing site. Tenants are per-newspaper or per-jurisdiction; local-government customers link outbound from the CMS. | Next.js SSR shell (`x-powered-by: Next.js`, `netlify-vary` + `x-nf-render-mode: ssr` headers) on Cloudflare edge; HTML references `enotice-production.firebaseapp.com` + `enotice-production.appspot.com` — Firebase/Firestore backend (internal project name "enotice"). Logos and notice attachments served from `firebasestorage.googleapis.com/v0/b/enotice-production.appspot.com/`. React/CRA chunks (`static/js/2.*.chunk.js`, `main.*.chunk.js`). The SPA's `<meta name="cf-2fa-verify">` tag and Stripe.js inclusion are characteristic. No plain-text `/robots.txt` served — the SPA shell returns HTML for every unknown path. | Data surface is **Firebase / Firestore** — raw Firestore REST would live under `firestore.googleapis.com/v1/projects/enotice-production/databases/(default)/documents/…`; the public notices SPA calls Firebase SDK (auth + Firestore listeners). ⚠️ GAP: direct collection paths not enumerated from anon curl — requires browser session to observe XHR. Tenant-scoped search URL `https://<tenant>.column.us/search` is the public entry. | _(none yet — see `lake-wales-fl.md`)_ |
| **ADG Building Department Portal** | American Data Group | `<tenant>.<apex>/permits` (observed: `secure.lakewalesfl.gov/permits`); mobile redirector `/permitsmu/`; citizenlink virtual dir `/adg/citizenlink` (403 anon). Sibling to ADG UBS (same vendor, same `/adg/supporting/` asset layout). | `<title>Building Department Portal</title>`; inline JS `ADG_BASE_URL="/adg"` + `ADG_CITIZENLINK_URL="/adg/citizenlink"` + `APPCODE="ADG"` + `SESSION=<sid>`; base64-encoded `<input id="common-object">` JSON carries `{SITENAME, USERCD:"BPSUSER", ORGCODE, webUrlMatch:"permits", redirector:"/permitsmu/", frameWork:"JQUERYUI"}`; assets under `/adg/supporting/jquery/{jqwidgets,datatables,uploader,select2,lobibox}/`; Apache + PHP/8.3; login-gated SPA with public-view mode for permit search (triggered via UI, not dedicated URL). | Login-gated SPA; `/permits` and `/permitsmu/` render the shell, `/adg/citizenlink` returns 403, `/permits/publicSearch.php` and common variants 404. Data XHR routes live behind session init. ⚠️ GAP: anonymous data surface not yet characterized — public-view mode likely requires in-SPA activation to assign a session cookie before XHRs resolve. Needs browser pass. | _(none yet — see `lake-wales-fl.md`)_ |
| **BoxCast** | BoxCast, Inc. | Embedded view pages on apex `boxcast.tv/view/<channel_slug>` + `boxcast.tv/view-embed/<channel_slug>`; public JSON API on `api.boxcast.com` (alias `rest.boxcast.com`); CloudFront-signed HLS / audio playlists on `play.boxcast.com/p/<channel_or_broadcast_id>/v/{all-ext,all-byteranges,audio-ext}.m3u8?Expires=…&Signature=…&Key-Pair-Id=…`; asset storage on `recordings.boxcast.com/YYYY/MM/DD/HHMMSS-<broadcast_id>/{posters,thumbs}/`; uploads on `uploads.boxcast.com/<account_id>/YYYY-MM/<path>/`; firebase realtime at `boxcastweb.firebaseio.com`. Observed account: `kpcnkyfgetmxappqpclx` (City of Bartow, FL). | Embedded view HTML contains inline JSON player config with `account_id` + `channel_id` + broadcast `id`. `api.boxcast.com/` returns `{"version":"9.8.0"}` (permissive `robots.txt` — fully allowed). Account IDs are 20-char lowercase-alphanum (e.g. `kpcnkyfgetmxappqpclx`); channel slugs are human-readable kebab-case with date suffix (e.g. `city-commission-meeting---4152024-iuza7m0are2vgrasvv6j`); broadcast IDs are 20-char lowercase-alphanum (e.g. `kwmmo9svs8cd7ia5tl4d`). | `GET api.boxcast.com/` (version), `GET api.boxcast.com/accounts/<account_id>` (→ account descriptor + default-channel HLS), `GET api.boxcast.com/accounts/<account_id>/channels` (→ channel array), `GET api.boxcast.com/channels/<channel_id>` (→ channel descriptor; also accepts a broadcast-id and returns broadcast-shape), `GET api.boxcast.com/channels/<channel_id>/broadcasts?q=&s=-starts_at&l=<limit>` (→ broadcast array), `GET api.boxcast.com/broadcasts/<broadcast_id>` (→ full broadcast metadata: name, description, starts_at, stops_at, streamed_at, time_zone, poster_url_template, thumbnail_url_template, tags, transcoder_profile), `GET api.boxcast.com/broadcasts/<broadcast_id>/view` (→ playback manifest with CloudFront-signed HLS URL). Singular `/account/<id>` paths return 404 — path is **plural**. | _(none yet — see `bartow-fl.md`)_ |
| **CivicPlus Municipal Drupal (ACSF)** | CivicPlus on Acquia Cloud Site Factory | Customer `.gov` / `.org` domains (observed: `www.eaglelakefl.gov`, `www.cityofmulberryfl.org`, `www.mypolkcity.org`). All Cloudflare-fronted. Tenants addressed internally by ACSF multi-site slot id (observed: `vyhlif14236` Eagle Lake, `vyhlif13676` Mulberry, `vyhlif10541` Polk City) — slot id is embedded in every static-asset path under `/sites/g/files/<vyhlif-id>/files/`. Internal ACSF template hostnames surface occasionally in misconfigured sitemaps (observed: `d9template1.test-civiccms.acsitefactory.com` leaked via Polk City's `/sitemap.xml`). | Acquia CDN headers: `x-ah-environment: 04live`, `via: varnish`, `x-cache: HIT`, `x-request-id: v-<uuid>`; asset prefix `/sites/g/files/vyhlif<id>/files/`; Drupal field markup (`field--name-field-agendas`, `field--name-field-minutes-files`, `field--name-field-smart-date`, `field--name-field-address`, `field--name-field-phone-number`); title pattern `<title>{Page Name} \| {City Name}</title>`. Drupal signatures (`Drupal` strings in HTML, `drupal-` CSS classes) but `generator` meta is empty (stripped by the CivicPlus template). `/wp-json/` 404s or WAF-403s (not WordPress). | `/robots.txt` (2,027-byte standard Drupal 9+ template with `/core/`, `/profiles/`, `/README.md` disallows + CSS/JS carve-outs); `/sitemap.xml` (simple_sitemap — beware stale hostname: Eagle Lake emits `eaglelake-fla.com` on all 564 `<loc>` values, Polk City emits only the ACSF template hostname; Mulberry's is correct); `/rss.xml` (default Drupal RSS, trailing ~10-item window, `<guid>{nid} at {hostname}</guid>` format, `<dc:creator>` exposes author); `/taxonomy/term/{tid}/feed` (per-taxonomy-term RSS; tid must be discovered separately); `/search?keys=…` (HTML search results; `/search/autocomplete/search_api_autocomplete` 404 — autocomplete disabled); `/print/pdf/node/{nid}` (entity_print PDF render); `/sites/g/files/vyhlif<id>/files/…` (binary file asset tree); `/meetings` (upcoming-meetings aggregator HTML, no JSON); `/{board-slug}/meeting/{meeting-slug}` (Meeting content type detail); `/{board-slug}/page/{page-slug}` (Page content type detail — some tenants model Special Magistrate hearings and meeting backups as Page nodes, not Meeting nodes); `/jsonapi` 404 (JSON:API disabled on the ACSF preset); `/node.json` 404; `/feed` 404 (use `/rss.xml`); `/directory-listing/{staff-slug}` under each department slug. | _(none yet — see `eagle-lake-fl.md`, `mulberry-fl.md`, `polk-city-fl.md`)_ |
| **Revize CMS** | Revize Software Systems | Customer domains (observed: `www.cityoffortmeade.org`; apex `fortmeadefl.gov` 301-redirects to it). Admin backend at `cms2.revize.com/revize/security/index.jsp?webspace=<tenant>` (observed `webspace=fortmeadefl`). | `.php` extensionless pages under `/departments/*` + root pages like `/calendar.php`, `/alert_detail.php`, `/index.php`; asset prefix `_assets_/` (relative); inline `<link rel="stylesheet" href="/revize/plugins/setup/css/revize.css">` + `<script src="/revize/util/snippet_helper.js">`; bundled plugins `jquery.bxslider`, `matchHeight`, `modernizr.custom`, `addtohomescreen`; security headers `X-Frame-Options: SAMEORIGIN`, `Content-Security-Policy: frame-ancestors 'self'`, `X-XSS-Protection: 1; mode=block`, `X-Content-Type-Options: nosniff`. Title-case URL typos are common and stable (observed: `city_commssion.php` — sic). | `/robots.txt` 302 (no directives), `/sitemap.xml` 404, `/rss` 404, `/feed` 404, `/wp-json/` 404, `/search` 404 — **Revize tenants emit no machine-readable site index**. The only structured artifacts are outbound links to vendor portals (BS&A Online, Municode Library, Column). `/revize/plugins/document_center/document_center_styles.css` suggests a document-center module exists; XHR contract not yet characterized. | _(none yet — see `fort-meade-fl.md`)_ |
| **BS&A Online** | BS&A Software | Customer tenants under the single apex `bsaonline.com`, addressed by numeric `uid` query parameter (observed: `uid=2524` Fort Meade City, FL). Cloudflare-fronted. Multi-tenant — `bsaonline.com/Home/MunicipalityDirectory` lists all tenants. | Application Gateway affinity cookies (`ApplicationGatewayAffinity`, `ApplicationGatewayAffinityCORS`); `ASP.NET_SessionId` session cookie; encrypted `IsWeb` cookie (`IsWeb=<hex>` gateway-level opaque tag). Initial `/?uid=<n>` GET 302-chains through `/Home/LandingPage` → `/?uid=<n>` → `/Home/MunicipalityHome?uid=<n>` and requires a cookie jar to settle. Title `"BS&A Online"`. Navigation includes "Services", "Pay a Bill", "Detailed Record Search", "Create an Account", "Utility Billing Record Search", "Pay a Utility Bill", "Go to Municipal Directory". | `/?uid=<n>` (302 chain to tenant landing), `/Home/MunicipalityHome?uid=<n>` (HTML landing; module tiles vary per tenant), `/Home/MunicipalityDirectory` (cross-tenant directory), `/SiteSearch/UtilityBillingRecordSearch?uid=<n>` (HTML utility-billing search form — GET renders form, POST submits search; JSON endpoint not yet characterized), `/BsaPayment/TakeUtilityBillingPayment?uid=<n>` (HTML payment form), other module paths vary per tenant (Tax, Assessment, Delinquent, Special Assessments, Cemetery — module enumeration is per-tenant). ⚠️ GAP: JSON API surface not yet discovered — BS&A may expose `/ApiServices/` routes on some tenants; deep probe requires browser pass. | _(none yet — see `fort-meade-fl.md`)_ |
